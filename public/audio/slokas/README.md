# Sloka audio

Use one directory per stable sloka content ID. Approved human chanting belongs
under `chant/`; generated narration belongs under `narration/<language>/generated/`.
Every playable file must have a corresponding reviewed entry in `../manifest.json`.
